#include <stdio.h>
#include <stdlib.h>

/**
 * main - Program that prints its name, followed by a new line
 * @argc : Count tab
 * @argv : Values of tab
 * Return: Return 0
 */

int main(int argc, char *argv[])

{
(void)argc;
	printf("%s\n", argv[0]);
		return (0);
}
