#include "main.h"
#include <stdio.h>
#include <ctype.h>
/**
 * print_diagonal - Print '\'
 * @n: Print numbers of '\'
 * Return: Always (Succes)
 */
void print_diagonal(int n)

{
int a;
int b;
int c;
        if (size <= 0)
        {
        _putchar('\n');
        }
a = 0;
	for (a = 0; a < size; a++)
	for (c = 0; < size; c--)
{
_putchar('#');
}
for (b = 0; b < size; b++)
{
_putchar(' ');
}
					}
	}
}
