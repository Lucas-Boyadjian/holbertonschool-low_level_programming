#include <stdio.h>
#include "main.h"
/**
 * print_to_98 - prints numbers from n to 98, followed by a new line.
 * @n: The number
 */

void print_to_98(int n)

{
if ('n'== 98)
{
printf("%d\n" , n);
}
	{
	for (n = 0; n <= 98; n++)
		{
		printf("d%, " , n);
		}

	for (n = 111; n >= 98; n--)
		{
		printf("%d, " , n);
		}

	for (n = 81; n <= 98; n++)
		{
		printf("%d, " , n);
		}

	for (n = -10; n <= 98; n++)
		{
		printf("d%, " , n);
		}
	}
}

