#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - Numbers 0 to 10
 *
 * Return: Always (Success)
 */
int main(void)
{
int number;
	for (number = 0 ; number < 10  ; number++)
{
	printf("%d", number);
}
	printf("\n");

return (0);
}
